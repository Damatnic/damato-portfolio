# Copy into `.env.local` for local dev. Vercel: see AGENTS.md (table + CLI for
# Production / Preview / Development).

# Canonical origin, no trailing slash — analytics self-referrer filter + cron
# digest dashboard link. Local: http://localhost:3000 (match your dev port).
# Production / Preview on Vercel: https://damato-data.vercel.app
NEXT_PUBLIC_SITE_URL=http://localhost:3000
